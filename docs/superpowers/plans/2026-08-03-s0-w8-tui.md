# S0/W8 TUI 体验 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把已经写好、有测试、连 i18n 文案都备齐、却**从未接进 TUI** 的偏好体系接通（`/keymap` `/vim` `/contrast` `/locale` 四个命令目前键入只得到 "unknown command"），修好 F1 帮助面板在小终端下**前 35 条命令不可见且无法滚动到**的缺陷，并落地服务端 `@path` 附件。

**Architecture:** 五个 PR。**串行关键路径只有一条：PR-3（UX3 服务端附件）→ PR-4（UX4 frecency 排序）** —— 在 UX3 落地前，UX4 的「近期选择靠前」**在任何 UI 上都不可观察**。其余可并行。

**Tech Stack:** Go 1.26.4，Bubble Tea（本地 fork），`internal/keymap` / `internal/i18n` / `internal/pathjail`。

---

## 本计划的写法

**这是意图级计划，不是代码清单。** 每步说清楚：改哪个文件的哪个函数、断言什么行为、怎么观察、为什么重要、预期看到什么。**具体代码由实现者写** —— 你手里有编译器，本文档没有。

「已核对事实」是**实测结果**，可直接依赖；其余标识符请先 grep。

---

## ⚠️ 三条硬约束（先读，它们决定任务顺序与落点）

### 1. `commands.go` 只剩 57 行余量，四个新命令**绝不能加进去**

`go run ./cmd/codelines` 实测：30 个非测试文件中**只有一个**超过 700 —— `internal/cli/tui/commands.go` **943 纯代码行**，限额 1000，且已在 GOV2 的 `approaching` 名单（`lines_test.go:46` 阈值 900）。次高 `model.go` 602、`view.go` 592、`entries.go` 588，都安全。

**`lineExceptions` 是空 map，没有任何豁免可用。**

四个 handler 加参数解析、诊断渲染、持久化错误处理，**稳超 57 行**。→ 必须新建 `internal/cli/tui/commands_prefs.go`（与既有 `commands_skills.go`/`commands_logs.go`/`commands_session_memory.go` 拆分风格一致），只在 `commands.go:87` 前的表里加四行条目。

> 💡 顺带删掉重复的 `features` 行（见下），**净增约 3 行**。
>
> ⚠️ **W7 Task 1 也要拆 `commands.go`**（把 `statsEntry` 拆出去）。**先落地的一方做，另一方 review 时确认** —— 不要拆两遍。

### 2. UX4 **必须**排在 UX3 之后

`TopN`（`frecency.go:79`）**零生产消费者**。它唯一可能的 UI 消费者就是 **UX3 的 `@path` 补全候选源** —— UX1 的 action palette 四源（`command`/`mode`/`model`/`theme`，`action.go:41-113`）里**没有文件源**。

**所以在 UX3 落地前，「近期选择靠前」在任何 UI 上都无法被观察。** spec §4.3 W8 **没有点出这条顺序依赖**。

### 3. CLAUDE.md 关于 SSE 的表述不准确 —— UX3 要改**四处**不是两处

CLAUDE.md 写「WS 与 SSE 共用同一套 JSON 帧词表……新增帧类型 → 同时更新 `ws.go` 与 `ssebackend.go`」。

⚠️ **这句对 `ServerFrame` 成立，对 `ClientFrame` 不成立。** 实测 `chat.go:42-57` 的 SSE 请求体是**匿名结构体**（Message/Messages/Model/Thinking/OutputSchema/ThreadID/TurnID），**从不 unmarshal `proto.ClientFrame`**。

| 侧 | 文件:行 | 改什么 |
|---|---|---|
| 服务端 WS | `ws.go:461` / `:1170` | 读 `cf.Attachments` |
| 服务端 SSE | `chat.go:42-57` / `:73` | 结构体加字段，**同一 resolver** |
| 客户端 WS | `internal/cli/wsbackend.go:68`（`Send`），`:86` 硬写 `proto.NewUserMessage(text)` | 需要能带附件的 turn 通道 |
| 客户端 SSE | `internal/cli/ssebackend.go:60`（`Send`），`:68-77` 匿名 body | 同上 |

> W1 Task 18 已把这条 CLAUDE.md 错误列入修正范围。**若 W1 已改，本计划确认即可。**

---

## 已核对事实（实测）

### C15：三个命令确实不存在

- `commandTable` 定义在 `commands.go:51`，字面量结束于 `:87`，**共 35 条**
- 临时探针调 `lookupCommand`（`commands.go:90`）：`keymap` / `vim` / `contrast` / `locale` **四个全部返回 `ok=false`**
- 唯一分发路径 `runCommand`（`commands.go:114`）查不到就走 `:121` 的 `errorEntry{text: "unknown command: /" + name}` —— **用户键入 `/keymap` 只会得到 "unknown command"**

⚠️ **审计未发现的缺陷**：`commands.go:64` 与 `:65` 是两条**完全相同**的 `{name: "features", ...}`。`help.go:41` 遍历 `commandTable` **不去重** → `/help`、Ctrl+K palette、`/` palette **全部显示两遍**。**实际只有 34 个不同命令。**

**底层零件全部已存在**：

| 资产 | 位置 |
|---|---|
| `keymap.Action` 语义常量 / `knownActions` | `internal/keymap/keymap.go:22`、`:25`、`:38` |
| `NewDefaultBuilder(overrides)` | `keymap.go:67` |
| `Map.Lookup(tea.KeyMsg)` | `keymap.go:189` |
| `Map.Diagnostics()` 四类诊断 | `keymap.go:202`，`Diagnostic` 类型 `:217` |
| `VimMachine` 状态机 | `vim.go:17/24/46/98` |
| `Preferences` 六字段 | `preferences.go:19-23`，`:27`（KeymapReset tombstone） |
| 四层合并 `mergeTUIPrefs` | `preferences.go:110` |
| 持久化 | `:53`/`:71`/`:145`（`os.UserConfigDir()/yanshi/prefs.json`）/`:157` |
| 高对比主题 | `styles.go:270`、`:293`、`:335` |
| **i18n 文案** | **全部已在 catalog 里**：`tui.command.help.keymap\|vim\|contrast\|locale`、`tui.command.keymap.{usage,conflict,none,reset}`、`tui.command.vim.{usage,enabled,disabled}`、`tui.command.contrast.usage`、`tui.command.locale.{usage,current,changed}`、`tui.command.preference.persist_failed` |

**断链的确切位置**：`mergeTUIPrefs` / `loadPreferences` / `persistPreferences` / `PreferencesFromEnv` **全仓非测试代码零调用点**。`newModel`（`model.go:291`）硬写 `prefs: Preferences{}` + `prefsPath: ""`（`:310-313`），`NewProgram`（`model.go:340`）**不接受任何 prefs 参数**。TUI 按键仍是 `handlers.go` 的硬编码 `switch msg.Type`（`:224` 起，Ctrl+K `:347`、Ctrl+S `:364`、F1 `:382`）。

### 其余七项 delta

| 项 | 缺口 | 位置 |
|---|---|---|
| **UX1** | Ctrl+K 开关、fuzzy 排序、Esc 关闭、10 行窗口**全部真实可用**；缺口只有一条 —— **会话源缺失**，注释明写 `session + tool/MCP source DEFERRED`，只收 command/mode/model/theme 四源，而验收要求「命令/模式/模型/**会话**」 | `action.go:40`、`:41`、`:118`、`:155`、`:198` |
| **UX2** | 内容三段防漂移、F1 开关、fuzzy 搜索**都真实**；缺口是 ⚠️ **`helpPopup` 完全没有行数上限与滚动** | `help.go:41`、`:57`、`:102`；`handlers.go:382` |
| **UX3** | 服务端 `@path` 附件**完全不存在**（verdict `missing`） | 见硬约束 3 |
| **UX4** | 存储路径是 `os.UserConfigDir()/yanshi/frecency.json`，格式是**单个 JSON 数组**非 JSONL；`TopN` **零生产消费者**；**确无禁用配置** | `frecency.go:166-172`、`:121`/`:128`、`:79` |
| UX4 | ⚠️ **`frecencyPath(root string)` 的 `root` 参数声明了但函数体内完全不用**，`newModel` 传了 `root` 进去（`model.go:325`）却被丢弃 —— **假装支持 per-project 而实际全局共享** | `frecency.go:166-172` |
| **UX8** | 四条验收在 TUI/编排层**全部成立**；缺口在 ⚠️ **provider 侧从未「开启」thinking** —— `anthropicRequest` 结构体**没有 `thinking`/`budget_tokens` 字段**（全仓 `rg budget_tokens` **零命中**） | `anthropic.go:137`；`reasoning.go:26` 只产出 `openai.WithReasoningEffort` |
| **E03** | 无 `/skill validate`（`cmdSkill` 的 switch 只有六个子命令，`default` 直接报 unknown） | `commands_skills.go:40-64`、`:59` |
| E03 | ⚠️ **重名冲突不可诊断** —— `Loader.Load` 在 `:188` 是 `continue // first-seen-wins`，**静默丢弃**被 shadow 的同名技能；`Registry` 里根本没有这些条目 | `internal/skills/skills.go:168`、`:188` |
| **I18N1** | TUI 硬编码英文 —— `defaultBundle()` 写死 `i18n.NewBundle("en")`，`cfg.I18N.UILocale` 与 `YANSHI_UI_LOCALE` **都进不了 TUI** | `state.go:119-120`；`model.go:291` |
| I18N1 | 65 个 key 里**只有约 22 个真被使用** —— 生产代码只有两处读 bundle | `commands.go:335`、`model.go:315` |

### UX2 的缺陷有多严重

`helpPopup`（`help.go:102`）**一次渲染全部 60+ 条**。对比 `action.go:198`（`maxRows = 10`）与 `history.go:192`（8 行）—— 只有它没有上限。

`view.go:181-183` 只用 `blockHeight` 记账**不裁剪**，最终由 `third_party/bubbletea/standard_renderer.go:186` 的 `newLines[len(newLines)-r.height:]` **从顶部截断** → **40 行终端下标题、"Commands:" 段头与前 35 条命令全部不可见且无法滚动到**。

---

## 五条裁定

**裁定 1 — C15 实现三个命令，不改文档。**

1. **改文档等于把已付出的成本一笔勾销。** `internal/keymap` 是完整、有测试的叶子包，`Preferences` 六字段 + 四层合并 + 原子持久化全写好，i18n catalog **连每句提示文案都备齐** —— 缺的只是 `commandTable` 里四行和把 `mergeTUIPrefs` 接进 `NewProgram`。**删文档保留死代码是最坏的组合。**
2. ⚠️ **`/keymap diagnostics` 已经是别处的对外承诺**：`internal/cli/doctor.go:646` 的失败文案**逐字写着** `"key bindings are invalid; use /keymap diagnostics"` —— 改 `tui.md` 而不改这里，**谎言只是换了个位置**；两处都改则 doctor **失去唯一可执行的补救指引**。
3. **`KeymapReset` tombstone（`preferences.go:27`）没有写入者** —— 这个字段的存在本身就是 `/keymap reset` 的规格说明。不实现就必须删字段，而删字段会让 `mergeTUIPrefs` 的 tombstone 分支（`:132-134`）**与其 doc 一起变成死代码**。

**配套**：`/locale` 一并做（I18N1 需要它，catalog 已备好）。**C15 与 I18N1 是同一条断链，两项应在同一个 PR 里一次接通。**

**文档侧仍要动**：`tui.md:27` 与 `configuration.md:93` 需补 `/locale`，`tui.md:19` 措辞需对齐。
> ✅ **这两处都在 GENERATED 块之外**（`tui.md` 生成块是 `48-90`，`configuration.md` 是 `99-286`），**手改安全，且 slash 命令不进 `-h`，不触发 docs 门禁**。

**裁定 2 — UX4 接受存储形态偏离，改验收标准。**

| 规划要的 | 实际 | 为什么接受 |
|---|---|---|
| `~/.yanshi/file-frecency.jsonl` | `os.UserConfigDir()/yanshi/frecency.json` | **converge 到 `~/.yanshi/` 是 Windows 上的实质回退** —— CLAUDE.md 明确本仓库在 Windows 上开发，`os.UserConfigDir()` 在 Windows 解析为 `%AppData%`，而 `~/.yanshi` 是 Unix 主义。当前路径还与同目录的 `prefs.json`（`preferences.go:150`）、`permModeFile` **保持一致** |
| JSONL | 单个 JSON 数组 | **JSONL 在这里买不到任何东西** —— 它的价值是增量追加，但 `Save()` 本来就是**全量重写 + 原子 rename**；换成 JSONL 只是多一条解析路径和多一种损坏形态，而 `LoadFrecency`（`:38`）现有的「坏 JSON 软降级为空」自愈逻辑**反而要重写** |

**迁移成本落在用户身上而收益为零。**

**改判后的验收**：「近期选择靠前」→「**`@path` 补全候选按 frecency 排序**」；「可禁用」→ 新增 `tui.frecency: true|false`。

⚠️ **新增 config key 会触发 docs 门禁，四个生成器必须重跑并提交。**

**裁定 3 — UX3 的附件引用只带路径，绝不带内容。**

`AttachRef` **只带 `Path string`** —— 内容由**服务端**读。**这正是被否决过的 MVP 与本方案的分界线。**

**guard fs 校验必须 fail-closed，三道**：
1. `pathjail.WithinRootAbs(workRoot, path)`（`internal/pathjail/pathjail.go:24`）—— **唯一 canonical root-jail**，含 symlink eval + Windows 盘符 `EqualFold` + 大小写复核。⚠️ **不得手写路径检查**
2. `guard.New().Check(prof, ...)` 走 `fs_read` 的 `checkFS`（`guard.go:227`）。profile 从 `Server.controlProfile`（`server.go:143`）取
3. ⚠️ **只有 `guard.Allow` 通过** —— 附件解析发生在 turn 开始**之前**、任何工具 context **之外**，**没有 permission callback 可升级**，所以 `Prompt` 与 `HardDeny` **一律当拒绝**。与 SSE 无 callback 时的 fail-closed 语义一致

**大小上限**：**单文件 64 KiB / 单 turn 合计 256 KiB**。超阈值**不读内容**，改为注入一行提示引导用 `fs_read` 分片。

> 理由：64 KiB 约 16k token，已是常见 128k 窗口的 **1/8**；再大就该走 `fs_read` 分片，否则**一次附件就能吃掉压缩预算**（W4 的量纲问题会被放大）。

**裁定 4 — E03 的 `/skill update` 明确「不做」，写进台账。**
MVP 用 uninstall + install，属计划内的**诚实声明**（YAGNI）。⚠️ 但要在台账 acceptance 里**显式记为「不做」而非「未做」** —— 两者对下一轮审计是完全不同的信号。

**裁定 5 — UX8 复用既有的 impl-option 结构体，不新造第二个。**
现成扩展点是 `outputSchemaOptions`（`outputschema.go:25`，被 `anthropic.go:210/:254` 与 `responses.go:299/:343` 解码）。

⚠️ **DRY 硬性要求**：`GetImplSpecificOptions` **按 setter 类型断言**，**两个结构体会互相看不见**。**绝不要新造第二个。**

---

## Task 1: 接通偏好体系 + 四个命令（PR-1 / C15 + I18N1）

> 裁定 1。**C15 与 I18N1 是同一条断链，同一个 PR 一次接通。**

**Files:** Create `internal/cli/tui/commands_prefs.go`；Modify `internal/cli/tui/{commands,model,state,handlers}.go`、`cmd/yanshi/main.go`（`:585 runTUI`）、`docs/user-guide/{tui,configuration}.md`

- [ ] **Step 1: 写失败测试**

**断言什么**：
1. `/keymap`、`/vim`、`/contrast`、`/locale` **都能被 `lookupCommand` 找到**（当前四个全返回 `ok=false`）
2. `/keymap diagnostics` **产出 `Map.Diagnostics()` 的四类诊断**
3. 偏好**跨进程持久化**：设置后重新构造 model 仍生效
4. `ui_locale` 从 `cfg.I18N.UILocale` 与 `YANSHI_UI_LOCALE` **都能进 TUI**
5. `/help` 里 `features` **只出现一次**

**为什么第 2 条重要**：`doctor.go:646` 的失败文案**逐字承诺**了 `/keymap diagnostics`。

**预期**：五条全 FAIL。

- [ ] **Step 2: 新建 `commands_prefs.go` 并注册四行**

⚠️ **绝不能加进 `commands.go`**（硬约束 1）。只在 `commands.go:87` 前的表里加四行条目。

**顺带删掉 `commands.go:64` 或 `:65` 的重复 `features` 行**（净增约 3 行）。

> ⚠️ 若 W7 Task 1 已拆过 `commands.go` 并删过重复行，**确认即可，不要重复做**。

- [ ] **Step 3: 接通四层合并**

`NewProgram`（`model.go:340`）接受 project `Preferences`（在 `cmd/yanshi/runTUI`（`main.go:585`）里 `config.Load` 后注入）；接通 `mergeTUIPrefs`；`keymap.Map` 接进 `handlers.go` 的 key 分发（替换 `:224` 起的硬编码 `switch msg.Type`）。

`defaultBundle()`（`state.go:119-120`）不再写死 `"en"`。

- [ ] **Step 4: 遵守 i18n 的两条不可违反约束**

⚠️ **`ui_locale: auto` 必须保持「每次启动重算、不持久化」**（`i18n.go:112-113` + `Persistent():138` 的 doc 已明确）。

⚠️ **`output_language`（`config.go:163`）是完全独立的旋钮，绝不能读 `ui_locale`** —— **模型看不见 TUI**。

⚠️ **`/locale` 写盘时必须写 `bundle.Persistent()` 而非 `Effective()`。**

- [ ] **Step 5: 同一 commit 里改文档**

`tui.md:19,27` 与 `configuration.md:93`。**文档与代码不得分离。**

> 两处都在 GENERATED 块之外，手改安全，不触发 docs 门禁。

- [ ] **Step 6: 全量与提交**

Run: `go run ./cmd/codelines` —— 确认 `commands.go` **没有变长**。

---

## Task 2: F1 帮助面板加上限与滚动（PR-2 / UX2）

> 不碰 `commands.go`，可与 Task 1 并行。

**Files:** Modify `internal/cli/tui/help.go`（`:102 helpPopup`）；Test

- [ ] **Step 1: 写失败测试**

**断言什么**：在一个**小终端高度**（如 40 行）下，帮助面板渲染出的行数**不超过窗口**，且**标题与第一条命令可见**。

**为什么重要**：当前一次渲染全部 60+ 条，`view.go:181-183` 只记账不裁剪，最终被 bubbletea 的 `newLines[len(newLines)-r.height:]` **从顶部截断** —— **标题、"Commands:" 段头与前 35 条命令全部不可见且无法滚动到**。

**预期**：FAIL。

- [ ] **Step 2: 实现**

**与另两个 popup 对齐**：`action.go:198` 是 `maxRows = 10`，`history.go:192` 是 8 行。加 `maxRows` + 光标滚动。

- [ ] **Step 3: 全量与提交**

---

## Task 3: Ctrl+K 补会话源（PR-2 / UX1）

**Files:** Modify `internal/cli/tui/action.go`（`:41 collectActions`）；Test

**背景：** Ctrl+K 的开关、fuzzy 排序、Esc 关闭、10 行窗口**全部真实可用**。缺口只有一条：`action.go:40` 的注释明写 `session + tool/MCP source DEFERRED`，`collectActions` 只收 command/mode/model/theme **四源**，而验收要求「命令/模式/模型/**会话**」。

- [ ] **Step 1: 写失败测试**

**断言什么**：palette 的候选里**包含会话**，且选中后能恢复该会话。

**预期**：FAIL。

- [ ] **Step 2: 实现**

**复用** `proto.NewSessionList` + `cmdRestore`。

**做完后把 `action.go:40` 的 DEFERRED 注释改成描述现实。**

> 💡 **文件源留给 Task 5**（UX4）—— 那是 UX4 的消费者。

- [ ] **Step 3: 全量与提交**

---

## Task 4: 服务端 `@path` 附件（PR-3 / UX3）

> **裁定 3。本工作包唯一 verdict 为 `missing` 的项，也是 Task 5 的前置。**
>
> 💡 **建议内部再切两刀**：先落服务端 resolver + frame + **越权/超限的负向测试**，再落 TUI 的 `@` 交互。

**Files:** Create `internal/api/http/attach.go`；Modify `internal/proto/frame.go`（`:40 ClientFrame`）、`internal/api/http/{ws,chat}.go`、`internal/cli/{backend,wsbackend,ssebackend,fakebackend}.go`、`internal/cli/tui/{model,images}.go`

- [ ] **Step 1: 先写负向测试（越权与超限）**

**断言什么**：
1. **越界路径被拒** —— `@../../etc/passwd` 之类
2. **`guard.Check` 返回 `Prompt` 时也当拒绝**（不只是 `HardDeny`）
3. **单文件超 64 KiB 不读内容**，改为注入引导用 `fs_read` 的提示
4. **单 turn 合计超 256 KiB** 同样降级

**为什么先写负向**：这条路径是**由用户文本触发的任意文件读取**，且发生在**任何工具 context 之外**（没有 permission callback 可升级）。**先有网再跳。**

**预期**：编译失败（`AttachRef` 不存在）。

- [ ] **Step 2: 加 frame 字段**

`internal/proto/frame.go:40` 的 `ClientFrame`，**紧邻既有的 `Images []ImageAttach`（`:78`，Tier G 的先例）** 新增附件字段。

⚠️ **`AttachRef` 只带 `Path string`，绝不带内容**（裁定 3）。

配套构造函数，对齐 `:118 NewUserMessageWithImages`。

- [ ] **Step 3: 写服务端 resolver**

**新建 `internal/api/http/attach.go`**，三道 fail-closed 校验按裁定 3。

⚠️ **必须用 `pathjail.WithinRootAbs`** —— 它是唯一 canonical root-jail（含 symlink eval + Windows 盘符 `EqualFold` + 大小写复核）。**不得手写路径检查。**

- [ ] **Step 4: 接两个服务端调用点**

- **WS**：`ws.go:461` 的 `runUserTurn` 闭包内，在 `resolveQuery`（`:467`）之后、`cs.history = append(...)`（`:473`）之前。`case "user_message"` 在 `ws.go:1170`
- **SSE**：`chat.go:39 handleSSEInternal`，请求结构体 `:42-57`，`resolveQuery` 调用 `:73`

⚠️ **SSE 的请求体是匿名结构体，从不 unmarshal `proto.ClientFrame`**（硬约束 3）—— 要**单独加字段**。

- [ ] **Step 5: 补客户端通道**

⚠️ **架构缺口**：`ChatBackend`（`internal/cli/backend.go:120`）的 `Send(ctx, text string)` **只能发纯文本**；`SendFrame`（`:122`）在 wsBackend 里会置 `controlMode = true`（`wsbackend.go:156`），**按 control-reply 而非 turn 关闭通道，不能拿来发 user_message**。

**必须动接口。** 实现方 4 个：`wsbackend.go:68`、`ssebackend.go:60`、`fakebackend.go:22`、`exec_test.go:164`。

**建议**：新增一个能带附件的 turn 发送方法，`Send(ctx, text)` **退化为薄包装（DRY）**；`tuiSession`（`model.go:23`）同步加一条。

💡 **`internal/cli/tui/images.go:11` 的 `buildSendFrame(text, images)` 是为这条路径预留的唯一 seam，目前零生产调用点** —— UX3 正好把它激活并扩展。

> ⚠️ **与 W1 Task 14 重叠**：W1 也要用 `buildSendFrame` 发图片。**先落地的一方建立通道，另一方复用。**

- [ ] **Step 6: 落 TUI 的 `@` 交互**

⚠️ **与 W1 Task 15 相关**：W1 只做了**服务端 `@path` 解析**，明确把 TUI 触发面留给本任务。**先确认 W1 的服务端解析与本任务的 resolver 是否重复** —— 若重复，**合并为一个**，不要两套。

- [ ] **Step 7: 全量与提交**

---

## Task 5: frecency 排序的 `@path` 补全（PR-4 / UX4）

> **硬约束 2：必须在 Task 4 之后。** 在 UX3 落地前，「近期选择靠前」在任何 UI 上都不可观察。

**Files:** Modify `internal/cli/tui/frecency.go`（`:166`、`:217`）、`internal/config/config.go`、`config.example.yaml`；Test

- [ ] **Step 1: 写失败测试**

**断言什么**：
1. `@path` 补全候选**按 frecency 排序**
2. **项目内过滤生效** —— A 项目不会推荐 B 项目的文件
3. `tui.frecency: false` 时**不记录也不排序**

**为什么第 2 条重要**：⚠️ `frecencyPath(root string)` 的 **`root` 参数声明了但函数体内完全不用**，而 `newModel`（`model.go:325`）**认真地传了进去** —— **假装支持 per-project 而实际全局共享**。审计漏报了这条。

**预期**：三条全 FAIL。

- [ ] **Step 2: 修死参数 + 加项目内过滤**

**新增一个按 root 过滤的 TopN 变体** —— 全局单文件会把 B 项目的文件推荐给 A 项目。

> ⚠️ **裁定 2**：**不改存储路径、不改 JSONL** —— 只修 root 过滤。

- [ ] **Step 3: 加 `tui.frecency` 配置**

⚠️ **新增 config key 会触发 docs 门禁** —— 四个生成器必须重跑并提交。

- [ ] **Step 4: 确认 `fs_mkdir` 幽灵名已清理**

⚠️ `frecency.go:217` 的 `case "fs_write", "fs_edit", "fs_mkdir"` 里 **`fs_mkdir` 是幽灵工具名**（从来不是注册工具）。

**这三处（`frecency.go:217`、`styles.go:406`、`entries.go:845`）都在 W1 Task 17 的范围内。**

- 若 W1 已清理 → **确认即可**
- 若未清理 → **本任务顺手清**，并在移交表里记明

💡 **顺带**：`extractPathFromToolArgs` 应**加 `apply_patch`** —— 它是真实写工具，frecency 现在漏记它。

- [ ] **Step 5: 把文件源接进 Ctrl+K palette**

**这是 `TopN` 的第二个消费者**，也让 Task 3 的 palette 更完整。

- [ ] **Step 6: 全量与提交**

---

## Task 6: 开启 Anthropic thinking（PR-5a / UX8）

**Files:** Modify `internal/llm/eino/{outputschema,anthropic}.go`；Test

**背景：** UX8 的四条验收在 **TUI/编排层全部成立**（`classify.go:363` 发 `proto.NewThinking`、`proto/frame.go:484` 独立帧类型、Ctrl+O 折叠 `handlers.go:338`）。缺口在 **provider 侧从未「开启」thinking**。

⚠️ `anthropicRequest`（`anthropic.go:137`）**没有 `thinking`/`budget_tokens` 字段** —— 全仓 `rg budget_tokens` **零命中**。而 `ReasoningEffortOption`（`reasoning.go:26`）**只产出 `openai.WithReasoningEffort`**，被 `orchestrator.go:510` 唯一使用。

- [ ] **Step 1: 写失败测试**

**断言什么**：给定 thinking 配置，发往 Anthropic 的请求体**含 `thinking` 对象与 `budget_tokens`**。

**预期**：FAIL —— 请求体里根本没有这个字段。

- [ ] **Step 2: 实现（裁定 5）**

**扩展 `outputSchemaOptions`**（`outputschema.go:25`）加一个 thinking 字段，在 `anthropic.go:297 buildRequest` 里映射成 Anthropic 的 `thinking{type:"enabled",budget_tokens:N}`。

⚠️ **绝不要新造第二个 impl-option 结构体** —— `GetImplSpecificOptions` 按 setter 类型断言，**两个结构体会互相看不见**。

- [ ] **Step 3: 全量与提交**

---

## Task 7: 技能校验与冲突诊断（PR-5b / E03）

**Files:** Modify `internal/skills/skills.go`（`:168 Load`、`:188`）、`internal/proto/frame.go`、`internal/api/http/ws_handlers.go`、`internal/cli/tui/commands_skills.go`（`:40-64`）；Test

**已有且真实**（不要重做）：`ParseInstallSource`（`install.go:37`）、`Install`（`:111`，同盘 staging → `rejectSymlinks`（`:235`）→ `isWithin` containment（`:250`）→ frontmatter + `validName`）、`Uninstall`（`:204`）；服务端 `handleInstallSkill`（`ws_handlers.go:188`）、`handleSkillMutation`（`:231`）、`handleListSkills`（`:172`）；帧词表 `proto/frame.go:857-916` 全套。

- [ ] **Step 1: 写失败测试**

**断言什么**：
1. `/skill validate [name]` **存在且能重验已安装技能**（当前 `cmdSkill` 的 switch 只有六个子命令，`default`（`:59`）直接报 unknown）
2. **同名技能冲突可诊断** —— `/skills` 显示被 shadow 的条目

**为什么第 2 条是改动面最大的一块**：`Loader.Load`（`skills.go:168`）在 `:188` 是 `continue // first-seen-wins`，**静默丢弃**被 shadow 的同名技能 —— **`Registry` 里根本没有这些条目**，`/skills` 只显示获胜者。

**预期**：两条 FAIL。

- [ ] **Step 2: 实现 validate**

新增一个 ClientFrame + 服务端 handler，**复用** `readFrontmatter`/`validName`/`validDesc`/`rejectSymlinks` —— 校验逻辑目前**只内联在 `Install` 里**，装完后无法主动重验。

- [ ] **Step 3: 实现冲突诊断**

`Load` 需**额外收集冲突信息**（名字、获胜来源、被 shadow 的来源与目录），经 `skills_list` 帧回传，`/skills` 渲染 shadowed 标记。

⚠️ **要动 `Loader`/`Registry` 的返回形状** —— 本项改动面最大的一块。

⚠️ **改 `ServerFrame` 要同时更新 WS 与 SSE**（这一条对 `ServerFrame` **成立**）。

- [ ] **Step 4: 全量与提交**

---

## Task 8: 台账翻牌 + 文档同步 + W8 收尾验证

- [ ] **Step 1: 翻牌 8 条**

| 条目 id | 现 verdict | 证据来自 |
|---|---|---|
| `C2/UX1` | partial | Task 3 |
| `C2/UX2` | partial | Task 2 |
| `C2/UX3` | **missing** | Task 4（含负向测试） |
| `C2/UX4` | partial | Task 5 |
| `C2/UX8` | partial | Task 6 |
| `C3/E03` | partial | Task 7 |
| `D3/C15` | partial | Task 1 |
| `D3/I18N1` | partial | Task 1 |

⚠️ **`C2/UX4` 的 acceptance 必须改写**（裁定 2）：「近期选择靠前」→「`@path` 补全候选按 frecency 排序」；「可禁用」→ `tui.frecency`；**存储形态偏离在 acceptance 里写明并注明理由**。

⚠️ **`C3/E03` 的 acceptance 必须把 `/skill update` 显式记为「不做」而非「未做」**（裁定 4）。

- [ ] **Step 2: 处理 W1/W7 移交的 `G/VISION-TOOL`**

W1 因「费用纳入 `/cost`」与「入口 B 的 TUI 触发面」两条未做而保持 `partial`；W7 Task 11 Step 2 处理 `/cost`。

**本任务完成 Task 4 Step 6 后，入口 B 的 TUI 触发面已具备。**

**做什么**：确认两条都已满足 → **翻牌 `G/VISION-TOOL`**；否则**诚实保持 `partial`** 并写明剩余项。

- [ ] **Step 3: 修 CLAUDE.md 的 SSE 表述**

⚠️ 「WS 与 SSE 共用同一套帧词表 → 同时更新两处」**只对 `ServerFrame` 成立**（硬约束 3）。

> 若 W1 Task 18 已改，**确认即可**。

- [ ] **Step 4: 报告 GOV1 的一条死条目**

⚠️ `internal/archtest/deps_test.go` 的 `portAllowlists` 里，`ip("internal/proto")` 的允许集列着 `ip("internal/pathjail"): true`，但 **`internal/proto` 当前完全不 import pathjail**（零命中）。

**这与 GOV2/4/5/6 的「豁免只减不增、死条目必须失败」原则相悖** —— 只是 **GOV1 的 allowlist 没有死条目检测**。

**做什么**：删掉这条死条目，并**记进移交表**建议治理层给 GOV1 补死条目检测。

> ⚠️ Task 4 会让 `internal/proto` 新增附件字段 —— **先确认它是否因此真的需要 pathjail**。若需要，**保留该条目并说明**。

- [ ] **Step 5: 全量验证**

```bash
go build ./... && go vet ./... && go test ./...
go run ./cmd/codelines                   # commands.go 必须仍在限额内
go test ./internal/archtest              # GOV1/GOV2 + 台账
go run ./cmd/gendocs -config docs/user-guide/configuration.md
go run ./cmd/gendocs -help-all docs/user-guide/tui.md docs/user-guide/entrypoints.md
go run ./cmd/api-schema -markdown docs/api/schema.md
go run ./cmd/api-schema -markdown docs/api/resources.md
git diff --exit-code docs/
```

- [ ] **Step 6: 提交**

---

## W8 验收清单

- [ ] `/keymap` `/vim` `/contrast` `/locale` **四个都能用**；`/keymap diagnostics` 产出四类诊断
- [ ] `/help` 里 `features` **只出现一次**
- [ ] 偏好**跨进程持久化**；`ui_locale: auto` **不持久化**
- [ ] `output_language` **不受 `ui_locale` 影响**
- [ ] 40 行终端下 F1 面板**标题与第一条命令可见**且可滚动
- [ ] Ctrl+K palette **含会话源与文件源**
- [ ] `@../../etc/passwd` 被拒；`guard.Prompt` **也当拒绝**；超 64 KiB 降级为提示
- [ ] `@path` 补全**按 frecency 排序**且**项目内过滤**
- [ ] 发往 Anthropic 的请求体**含 `budget_tokens`**
- [ ] `/skill validate` 可用；同名冲突在 `/skills` 里**可见**
- [ ] `commands.go` **仍在限额内**（`codelines` 不报 approaching）
- [ ] 四个文档生成器跑完 `git diff --exit-code docs/` 无输出

## 依赖与移交

| 事项 | 关系 |
|---|---|
| **Task 4 → Task 5** | UX3 是 UX4 唯一可观察的消费者（硬约束 2） |
| `commands.go` 拆分 | **与 W7 Task 1 重叠**，先落地的一方做 |
| `buildSendFrame` 通道 | **与 W1 Task 14 重叠**，先落地的一方建立 |
| `@path` 服务端解析 | **与 W1 Task 15 重叠**，若重复则**合并为一个** |
| `fs_mkdir` 三处清理 | **与 W1 Task 17 重叠** |
| GOV1 缺死条目检测 | **治理层** |
| `/skill update` | **不做**（裁定 4），台账记为「不做」 |

## 审计 / spec / CLAUDE.md 中已被证伪的论断（不要照着做）

1. ⚠️ **CLAUDE.md 关于 SSE 的表述只对 `ServerFrame` 成立** —— UX3 要改**四处**而非两处。
2. **审计对 `commandTable` 的计数掩盖了一个 bug** —— 「共 35 项」数字对，但 `:64` 与 `:65` 是同一条 `features` 重复两次，**实际只有 34 个不同命令**。
3. **审计漏报 `frecencyPath` 的死参数** —— 声明了 `root` 却完全不用，而 `model.go:325` 认真地传了进去。**这直接导致 frecency 全局共享、跨项目污染，对 `@path` 补全是实质性错误。**
4. ⚠️ **`deps_test.go` 的 `portAllowlists` 有一条死条目**（`internal/proto` → `internal/pathjail`）。
5. **spec §4.3 W8 没有点出 UX4 对 UX3 的顺序依赖。**
6. ⚠️ **`fs_mkdir` 幽灵名在 `frecency.go:217` 仍在** —— W1 与 W8 在这三行上有重叠。
