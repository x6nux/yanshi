# S0/W10 发布就绪 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让发布流水线**第一次真的跑一遍**（本仓库至今一个 tag 都没有），补上两条开源首发的硬阻塞（**没有 LICENSE**、SECURITY.md 联系方式自标 placeholder），修好会让第二次发布**丢失整个 CHANGELOG 历史**的 `--latest` bug，并给覆盖率与两个隐藏子命令加上门禁。

**Architecture:** 四条链路：① 发布流水线 dry-run + 两个 `release.yml` 真实缺陷；② 覆盖率门禁（新 dev 工具 + CI job，**不放 archtest**）；③ usage↔dispatch 一致性断言；④ 文档与仓库卫生。

**Tech Stack:** Go 1.26.4，GoReleaser v2.7，git-cliff。

---

## 本计划的写法

**这是意图级计划，不是代码清单。** 每步说清楚：改哪个文件的哪个函数、断言什么行为、怎么观察、为什么重要、预期看到什么。**具体代码由实现者写** —— 你手里有编译器，本文档没有。

「已核对事实」是**实测结果**，可直接依赖；其余标识符请先 grep。

---

## ⚠️ 两条 spec 完全漏掉的开源首发硬阻塞

| 阻塞 | 实测 |
|---|---|
| **仓库没有 LICENSE 文件** | `ls LICENSE*` **无匹配** |
| `SECURITY.md` 的联系邮箱是自标的占位符 | 原文：`security@x6nux.dev *(placeholder — replace before going public)*` —— **文件自己写着「公开前替换」** |

**按 spec §2 约束 4「开源产品标准」，这两条比表内任何一项都更该在 W10 收口。**

> LICENSE 补上后，`.goreleaser.yaml` 的 `archives.files`（现为 `config.example.yaml` + `README.md`）**必须同步加一行**。

---

## 已核对事实（实测）

### 覆盖率：审计的数字是陈旧的

```
go test -cover ./internal/proto/... ./internal/bootstrap/... ./internal/store/...
```

| 包 | 实测 | spec 目标 | 审计写的 |
|---|---:|---:|---:|
| `internal/proto` | **100.0%** | 80% | — |
| `internal/bootstrap` | **94.1%** | 50% | — |
| `internal/store` | **95.5%** | 75% | — |

⚠️ **审计 COV3 标题写的「23%」是陈旧数字**，可溯源到 `docs/archive/synthesis-report-v2.md:343`（2026-07 快照）。同理 COV2 的「67%」实测 100%。

> **三个数全部远超目标 → 覆盖率子条件今天就满足，缺的只有门禁。两项的真实缺口不是覆盖率，而是测试语义。**

```
rg -n -- '-cover' .github/workflows/      → NO MATCH（零命中）
rg -n 'cover' internal/archtest/          → 仅 3 处散文命中
```

**全仓无覆盖率门禁，任何一处从 94% 掉到 20% 都不会红。**

### 两个隐藏子命令，不是一个

- `cmd/yanshi/main.go:38 var usage`，正文到 `:77`
- usage 的 Subcommands 段列出 **8 项**：`(none)`、`chat`、`exec`、`serve`、`app`、`goal`、`vcs-mcp`、`doctor`
- 实际可分发（`main.go:110-136` 的 switch + `:245-256` 的 managed dispatcher）：**9 项 + 裸调用** —— 多出 **`pr`**（`:123`）与 **`auth`**（`:249`）

⚠️ **审计只抓到 `auth`。**

**旁证**：`cmd/gendocs/help.go:30` 的 `yanshiSubcommands` 列全了 10 项（含 `pr`/`auth`），`gendocs_test.go` 也断言它与 main.go 的 switch 同步 —— 但**没有任何断言把 `usage` 文本和 dispatch 对起来**。所以 `entrypoints.md` 里 `pr`/`auth` 的 `-h` 快照是有的，唯独顶层 `yanshi -h` 看不到它们。

### PKG1 已修，且 `goreleaser check` 已进 CI

- `.goreleaser.yaml` **没有 `changelog:` 段了**。取而代之是一段 8 行注释，明确解释「不写 changelog 段」与「不能写 `changelog.disable: true`」的区别：disable 检查在 pipe 的 `Skip()` 里而非 `Run()` 里，禁用会连带跳过 `--release-notes` 的加载，**发出空 body 的 Release**
- 版本已 pin：`release.yml` 与 `ci.yml` 都是 `version: "~> v2.7"`（下限理由是 `archives.format_overrides` 用复数 `formats` 键）
- ✅ **`ci.yml` 有 `release-config` job（硬门禁）跑 `goreleaser-action@v6 … args: check`**，注释写明「配置错误应在 PR 上暴露，而不是在 tag push 那一刻」

> **PKG1 的「配置会炸」这一条已结案。** 剩余缺口是**流水线从未真跑过** + 归档物缺 LICENSE。

### 流水线从未被触发过

```
which goreleaser → not found
which git-cliff  → not found
git tag | wc -l  → 0
git tag -l 'v[0-9]*' | wc -l → 0
```

⚠️ **本仓库连 m1..m9 里程碑 tag 都没有（一个 tag 都没有）。** `build.sh` 的「无 `v*` tag 回落」分支是当前**唯一走过的路径**。

> ⚠️ **spec §5.1 与 VER1 证据都称「只有 m1..m9 里程碑 tag」** —— 与实测不符。`build.sh` 与 `docs/upgrade-guide.md:64` 里「里程碑标签 m1..m9 被刻意跳过」的表述**目前是对不存在对象的描述**。

### `release.yml` 的两处真实缺陷（属 VER1）

**① `git-cliff --latest --output CHANGELOG.md`**
`--latest` 只处理最新 tag 区间，`--output` 是**覆盖写**。第一次发布碰巧没事（无前序 tag = 全历史），**第二次发布会把 CHANGELOG.md 整个替换成只有 v2 那一节，v1 的历史丢失**。

> RELEASE_NOTES.md 用 `--latest` **是对的**，**CHANGELOG.md 必须去掉 `--latest`**。

**② 「commit changelog」步骤在 goreleaser 之前，且做 `git push origin HEAD:main`**
tag push 时 checkout 是 **detached HEAD**；只要 main 已往前走过，这个 push 就是 non-fast-forward → 步骤失败 → **整个 Release 发不出去**。

> **changelog 回写绝不该阻断产物发布** —— 应移到 goreleaser **之后**，并允许其失败。

### cliff.toml 与提交历史

历史规模：`git rev-list --count HEAD` = 32（压缩过的干净历史，root 是 `700d83a chore: init repo with design spec`）。

前缀分布：`11 test · 7 docs · 5 feat · 4 fix · 2 chore · 1 refactor · 1 ci · 1 style`

⚠️ **问题一：`style` 不在任何 parser 里。** `28024f2 style: gofmt the whole tree` —— `cliff.toml` 的 `commit_parsers` 没有 `^style`，`docs/commit-convention.md` 的前缀表也没有。配置是 `filter_unconventional=false` + `filter_commits=false`，该提交会带着**空 group** 进模板，而 body 里的 `commits | group_by(attribute="group")` 对无该属性的对象是**静默跳过**的 → **这条提交在 CHANGELOG 里凭空消失，不报错**。

⚠️ **问题二：`docs/commit-convention.md` 自身已漂移。** "Scope" 段把 `ide-vscode` 列为推荐 scope —— **W0 已经把 `ide/vscode/` 整个删掉了**（`removal_test.go` 在守）。

现有 `CHANGELOG.md`（18 行）是**手写种子**，末尾明确写着「第一个 tag 会用 git-cliff 生成体替换本种子」—— 逻辑成立，但**要先修掉 `--latest` 那个覆盖写 bug，否则替换后第二次发布就丢历史**。

### `docs.yml` 的 paths

push 与 pull_request 两处**完全相同**，均为 7+1 条：`docs/**`、`examples/**`、`cmd/api-schema/**`、`cmd/gendocs/**`、`internal/docgen/**`、`internal/config/**`、`internal/api/v1/**`、`.github/workflows/docs.yml`。

⚠️ **不含 `cmd/yanshi/**`** —— CLI 帮助快照的真相源是 `cmd/yanshi/{main,app,headless}.go` 里的 FlagSet（`rg -l NewFlagSet` 只命中这三个文件），**改 flag 文案的 PR 不会触发 docs job，`help:*` 快照静默漂移**。

⚠️ **同类缺口**：**`sdk/**` 也不在 paths 里**（W9 Task 8 处理）。

> 顺带核实：四个生成器当前**无 diff**（跑了全部四条命令 + `git diff --stat docs/`，输出 DOCS-CLEAN）。

### `nightly.yml` 的软门禁与两个内部逃生口

`nightly.yml:18` 的 `continue-on-error: true # soft until E2 fuzz targets land` **仍在**。

实测 `go test -list 'Fuzz.*' ./internal/guard/...` 输出 `FuzzMatchGlob` —— **注释写的那个前提条件已经成立**。

⚠️ **同一失效模式还藏在 shell 里而不是 YAML 键上**：
- `nightly.yml` fuzz-long 步骤里的 `if ! go test -list … ; then echo "…soft-pass"; exit 0; fi`
- `ci.yml` fuzz-seed 步骤里同款的 `echo "no fuzz/property targets yet (E2 pending); soft-pass"; exit 0`

**两处都意味着「有人删光 fuzz/property 目标 → job 照样绿」。**

> E2 资产（`FuzzMatchGlob` + `internal/ctxcompact` 的 8 个 `TestProperty_*`）**已实测存在**。

### 三份文档 / 仓库卫生

| 项 | 缺口 |
|---|---|
| **CONTRIB1** | `CONTRIBUTING.md` 66 行，8 个承重约定段齐全，引用的符号全部真实可达 |
| CONTRIB1 | ⚠️ **完全没提机器强制治理** —— W0 刚把 `governance` 变成硬门禁并新增 GOV4/5/6 + 台账断言，CONTRIBUTING 里对 `internal/archtest`、`cmd/codelines`、三张豁免表、`cmd/featurestatus` **零字提及**；也没提「改 config/`-h`/schema 必须重跑四个生成器并提交」。**贡献者照着 CONTRIBUTING 走会在 governance/docs 两个 job 上撞墙** |
| CONTRIB1 | ⚠️ `docs/archive/README.md` 的映射表**只有 3 条**，但目录里有 7 个 md；末尾断言 synthesis-* 三份与 `deps_analysis` 「为未跟踪文件，故不在本目录」—— **两处都假**：三份 synthesis-* 就在 `docs/archive/` 且已被 git 跟踪；`deps_analysis.md` 也被跟踪，只是躺在**仓库根**没归档。`feature-roadmap-e-h.md` 在目录里却不在表里 |
| CONTRIB1 | ⚠️ **仓库根有 4 个已跟踪的垃圾文件**会随开源仓库暴露给所有人：`demo.txt`（还是 dirty 状态）、`diffdemo.txt`（一段贴进来的 agent 输出）、`deps_analysis.md`、`deps_analyze.py`。`git ls-files \| grep -v /` 可复现 |
| **EX1** | 7 个示例（≥5 达标），全部 `--fake-model` 零 key；docs.yml 覆盖了 custom-tool build、headless exec/batch、TS typecheck、Python parse+import |
| EX1 | ⚠️ **`examples/sdk-python/main.py:25` 运行期必崩** —— `item.toolName` 不是属性（`generated.py:121` 是 `tool_name` + alias），**alias 只作输入键**。CI 只做 `py_compile` + `import`，**抓不到** |
| EX1 | `examples/goalloop-config/run.sh` 存在且自带断言，但**不在 docs.yml 任何一步里**；`custom-skill/reverse-echo/SKILL.md` 也没有结构字段断言 |
| **UDOC1** | `docs/user-guide/` 8 页齐全，索引可达，getting-started 零依赖四步；`entrypoints.md` 的 10 个 `help:*` 生成块**包含 `pr` 与 `auth`** |
| UDOC1 | ⚠️ **根 README.md（254 行）对 `docs/user-guide`、`docs/api`、`docs/adr`、`examples/`、`CONTRIBUTING.md` 的引用数为 0**（实测 `grep -cE` = 0，只提了 `docs/skills-authoring.md` 与 `docs/vcs.md`）→ **开源发布后陌生人从 README 找不到用户指南、贡献指南、示例** |
| UDOC1 | `tui.md` 与 `configuration.md` 仍宣称 `/keymap` `/vim` `/contrast` 可用（**W8 负责修，W10 负责加门**） |

### COV2 / COV3 的真实缺口是测试语义

**COV2**：审计说「31 个 `ServerFrame` Type」—— AST 实测是 **34**（33 个字符串字面量 + 1 个 `SubagentEventType` 常量；`goldenFrames()` 35 行覆盖 34 型，`status` 有两个构造器）。

但审计指出的**两个真实弱点成立且已复现**：
- `goldenFrames()` 里 `NewTaskUpdate(nil)` 因 `frame.go:726` 的 nil 短路返回零值 → golden 第 **82-83 行**冻结的是 `event: `（空）/ `data: {"type":""}`
- `TestVocabulary_Symmetry`（`frame_test.go:501`）断言 `f.Type == event`，而 `SSEEvent()`（`frame.go:669-673`）**第一行就是 `event = f.Type`** —— **恒真重言式**

> 💡 **好消息**：一条「golden 实际发出的 Type 集合 == `frame.go` 声明的 34 个 Type」的断言**今天就是红的**（缺 `task_update`、多一个空串），**是干净的 TDD 起点**。

**COV3**：审计指的两条弱测试**属实** —— `TestBuild_VCSSoftDegrade`（`bootstrap_test.go:104`）只调 `buildMinimalApp` 后断言 `app.VCS != nil`，**根本没制造失败**。

**补充实现路径**：`bootstrap.go:519` 的 `workRoot, _ := os.Getwd()` 是硬编码 cwd，**导致该分支不可注入**。最小修法是给 `Options`（`bootstrap.go:175`，已有 `Cfg`/`ProviderBuilder`/`AuthDeps` 三个同类测试 seam）加一个带 doc 注释的 `WorkRoot string`，指向不存在的目录即可让 `canonicalRepoRoot`（`vcs.go:521` 的 `EvalSymlinks`）失败，**跨平台可靠**。

---

## 四条裁定

**裁定 1 — 覆盖率门禁用 CI job + `cmd/covercheck`，不放 archtest。**

**阈值**取 `max(spec 验收值, 实测值-3pp)` → **proto 97 / store 92 / bootstrap 91**（既满足 spec 契约，又是真正的 ratchet，**不给 44 个百分点的免费退化空间**）。

**为什么不放 archtest**：
1. ⚠️ **递归与自指** —— archtest 断言要 `go test -cover` 就得在 `go test` 里**再起 `go test`**；范围一旦写成 `./...` 直接**自我递归**，写死三个包又是另一张需维护的表，且 `-race` job 下会变成嵌套非 race 运行
2. **代价与重复** —— `go test ./...` 跑在三平台，archtest 版本会把 bootstrap（11.3s）+ store（6.4s）的嵌套测试**重复三遍**；bootstrap 的测试会真起 sqlite 与 `127.0.0.1:0` 监听，**嵌套在另一个持有临时目录的 `go test` 里是端口/lockfile 争用的邀请函**
3. **类别不同** —— GOV1-GOV6 全是对**源码结构**的静态断言（AST / `go list`），覆盖率是 `go test` 的**运行产物**。塞进同一张桌子会让「archtest = 结构治理」这个清晰边界糊掉；而 **CI job 同样是机器强制的，并不比 archtest 弱**

⚠️ **首次 CI 运行后按 ubuntu 实测值复核阈值**（本次只在 macOS 测过）。

**裁定 2 — `nightly.yml:18` 的 `continue-on-error` 在本包收紧，bench 那条保持软。**

fuzz 的注释写着 "soft until E2 fuzz targets land"，而 `FuzzMatchGlob` **已实测存在** —— **前提条件已经成立**，留着就是又一条「文档描述与现实不符」。而且 nightly 本来就不阻塞合并，`continue-on-error` 唯一作用是**把红色变成静默**，**恰好抹掉 fuzz 这类「价值 100% 在告警」的 job 的全部价值**。

**第 38 行的 bench 保持软** —— 它是 trend-only、全树跑无 pass/fail 语义（与 W7 裁定 3 一致）。

**两个内部逃生口一并处理**：`nightly.yml` 与 `ci.yml` 的 `soft-pass; exit 0` 都改成「**目标缺失即失败**」。

**裁定 3 — 发布流水线必须真跑一次 snapshot，不能只靠 `goreleaser check`。**
`check` 只验配置能解析；本仓库**一个 tag 都没有**，产物是否能构建、归档物是否齐全、`CGO_ENABLED=0 -tags=nokeyring` 的二进制是否真能跑，**全部未经验证**。

**裁定 4 — `cliff.toml` 补 `^style` 而非禁用该前缀。**
历史里已有 `28024f2 style: gofmt the whole tree`，且 `style` 是 conventional commits 的**标准前缀之一**。禁用它意味着要么改写历史、要么让那条提交永远消失。**补 parser 更便宜也更正确**，同步更新 `docs/commit-convention.md` 的前缀表。

---

## Task 1: 补 LICENSE 与 SECURITY 联系方式（开源首发硬阻塞）

**Files:** Create `LICENSE`；Modify `SECURITY.md`、`.goreleaser.yaml`

- [ ] **Step 1: 确认现状**

Run: `ls LICENSE*` → **无匹配**
Run: `grep -n placeholder SECURITY.md` → 命中自标的占位符行

- [ ] **Step 2: 补 LICENSE**

⚠️ **协议选择是产品决策** —— 若仓库没有既定选择，**停下来汇报**，不要替用户选。

**线索**：`go.mod` 的模块路径、`README.md` 的表述、`SECURITY.md` 的措辞可能已隐含意图。

- [ ] **Step 3: 同步 `.goreleaser.yaml` 的 `archives.files`**

现为 `config.example.yaml` + `README.md` —— **补 LICENSE 一行**，否则用户下载的归档里没有协议文件。

- [ ] **Step 4: 换掉 SECURITY.md 的占位邮箱**

⚠️ **联系方式同样是产品决策** —— 不确定就**停下来汇报**。

- [ ] **Step 5: 提交**

---

## Task 2: 发布流水线 dry-run（裁定 3 / PKG1 + VER1）

**Files:** 无（验证性任务），产出记录进 commit message 或 notes

- [ ] **Step 1: 装工具**

```bash
go install github.com/goreleaser/goreleaser/v2@v2.7.0     # 与 CI 同一 pin
```

⚠️ **必须与 `release.yml`/`ci.yml` 的 `version: "~> v2.7"` 同 pin** —— 用不同版本 dry-run 等于没测。

- [ ] **Step 2: 跑 check 与 snapshot**

```bash
goreleaser check
goreleaser release --snapshot --clean --skip=publish
```

- [ ] **Step 3: 逐条肉眼核验产物**

```bash
ls dist/*.tar.gz dist/*.zip     # 恰好 4 个
ls dist/checksums.txt           # 存在且 4 行 sha256
tar tf <任一 tar.gz>            # 含 yanshi + config.example.yaml + README.md + LICENSE
# 解包后：
./yanshi -h                     # 退出 0
```

⚠️ **最后一条是 `CGO_ENABLED=0 -tags=nokeyring` 产物的唯一真实冒烟** —— `goreleaser check` 验不到它。

⚠️ **确认 LICENSE 在归档里**（Task 1 Step 3 的验收）。

- [ ] **Step 4: 把 snapshot 加进 `nightly.yml`**

加 `workflow_dispatch`，让「配置能解析」升级为「**产物能构建**」的每日守卫。

- [ ] **Step 5: 提交**

---

## Task 3: 修 `release.yml` 的两处真实缺陷（VER1）

**Files:** Modify `.github/workflows/release.yml`

- [ ] **Step 1: 去掉 CHANGELOG.md 的 `--latest`**

⚠️ `--latest` 只处理最新 tag 区间，`--output` 是**覆盖写**。第一次发布碰巧没事（无前序 tag = 全历史），**第二次发布会把 CHANGELOG.md 整个替换成只有 v2 那一节，v1 的历史丢失**。

> **RELEASE_NOTES.md 用 `--latest` 是对的** —— 只改 CHANGELOG.md 那条。

- [ ] **Step 2: 把 changelog 回写移到 goreleaser 之后并允许失败**

⚠️ tag push 时 checkout 是 **detached HEAD**；只要 main 已往前走过，`git push origin HEAD:main` 就是 **non-fast-forward** → 步骤失败 → **整个 Release 发不出去**。

**changelog 回写绝不该阻断产物发布。**

- [ ] **Step 3: 提交**

---

## Task 4: `cliff.toml` 补 `^style` + 修 commit-convention 漂移（裁定 4）

**Files:** Modify `cliff.toml`、`docs/commit-convention.md`

- [ ] **Step 1: 补 parser**

⚠️ 配置是 `filter_unconventional=false` + `filter_commits=false`，所以 `style:` 提交会带着**空 group** 进模板，而 `commits | group_by(attribute="group")` 对无该属性的对象是**静默跳过**的 → **那条提交在 CHANGELOG 里凭空消失，不报错**。

- [ ] **Step 2: 同步 `docs/commit-convention.md` 的前缀表**

- [ ] **Step 3: 修 commit-convention 的 scope 漂移**

⚠️ "Scope" 段把 `ide-vscode` 列为推荐 scope —— **W0 已经把 `ide/vscode/` 整个删掉了**（`removal_test.go` 在守）。**这是 W10 文档扫描该抓的第一条。**

- [ ] **Step 4: 提交**

---

## Task 5: 覆盖率门禁（裁定 1 / COV2 + COV3 的门禁部分）

**Files:** Create `cmd/covercheck/`；Modify `.github/workflows/ci.yml`、`CLAUDE.md`、`CONTRIBUTING.md`

- [ ] **Step 1: 写 `cmd/covercheck`**

**做什么**：阈值表 + 解析 `coverage: NN.N% of statements`，低于阈值即失败。

**阈值**：proto 97 / store 92 / bootstrap 91（裁定 1）。

**为什么要能本地跑**：一条命令 `go run ./cmd/covercheck` 可复现，贡献者不必等 CI。

⚠️ **GOV3 要求新导出符号必须有 doc 注释。**

- [ ] **Step 2: 在 `ci.yml` 加 `coverage` 硬门 job**

- [ ] **Step 3: 登记进 CLAUDE.md 的 dev 工具清单与 CONTRIBUTING**

- [ ] **Step 4: 首次 CI 运行后复核阈值**

⚠️ **本次只在 macOS 测过** —— 按 **ubuntu 实测值**复核，避免阈值定得比实际还高导致必红。

- [ ] **Step 5: 提交**

---

## Task 6: 修两条弱测试的语义（COV2 + COV3 的真实缺口）

**Files:** Modify `internal/proto/frame_test.go`（`:501`）、`internal/proto/` 的 golden、`internal/bootstrap/{bootstrap.go,bootstrap_test.go}`（`:175`、`:519`、`:104`）

> ⚠️ **两项的真实缺口不是覆盖率**（实测 100% / 94.1%），**而是测试语义。**

- [ ] **Step 1: 写「golden 覆盖全部 Type」的断言**

**断言什么**：golden 实际发出的 Type 集合 **== `frame.go` 声明的 34 个 Type**。

💡 **这条今天就是红的**（缺 `task_update`、多一个空串）—— **干净的 TDD 起点**。

**根因**：`goldenFrames()` 里 `NewTaskUpdate(nil)` 因 `frame.go:726` 的 nil 短路返回零值 → golden 第 **82-83 行**冻结的是 `event: `（空）/ `data: {"type":""}`。

- [ ] **Step 2: 修 `TestVocabulary_Symmetry` 的恒真重言式**

⚠️ 它断言 `f.Type == event`，而 `SSEEvent()`（`frame.go:669-673`）**第一行就是 `event = f.Type`** —— **这条测试不可能失败**。

**改成什么**：断言一个**独立来源**的期望（如声明表），而不是被测函数自己刚赋的值。

- [ ] **Step 3: 给 `Options` 加 `WorkRoot` seam 并修 `TestBuild_VCSSoftDegrade`**

⚠️ 该测试只调 `buildMinimalApp` 后断言 `app.VCS != nil` —— **根本没制造失败**，所以「软降级」这条路径**从未被执行过**。

**实现路径**：`bootstrap.go:519` 的 `workRoot, _ := os.Getwd()` 是硬编码 cwd，**导致该分支不可注入**。给 `Options`（`bootstrap.go:175`）加一个带 doc 注释的 `WorkRoot string`（已有 `Cfg`/`ProviderBuilder`/`AuthDeps` 三个**同类测试 seam** 的先例），指向不存在的目录即可让 `canonicalRepoRoot`（`vcs.go:521` 的 `EvalSymlinks`）失败 —— **跨平台可靠**。

- [ ] **Step 4: 全量与提交**

---

## Task 7: usage ↔ dispatch 一致性断言（两个隐藏子命令）

**Files:** Modify `cmd/yanshi/main.go`（`:38 usage`）；Test `cmd/yanshi/`

- [ ] **Step 1: 写失败测试**

**断言什么**：`usage` 文本里的 Subcommands 列表与实际 dispatch（`main.go:110-136` 的 switch + `:245-256` 的 managed dispatcher）**双向一致** —— 既不能少列，也不能列出不存在的。

**为什么重要**：`gendocs_test.go` 已断言 `yanshiSubcommands` 与 switch 同步，但**没有任何断言把 `usage` 文本和 dispatch 对起来** —— 所以 `pr` 与 `auth` 在顶层 `yanshi -h` 里**隐身至今**。

**一条测试同时销掉两个隐藏命令。**

**预期**：FAIL，少了 `pr` 与 `auth`。

- [ ] **Step 2: 补 usage 文本**

- [ ] **Step 3: 重跑 gendocs 并提交**

⚠️ 改了 `-h` 文本 → `docs.yml` 的 diff gate **必定**产生 diff：

```bash
go run ./cmd/gendocs -help-all docs/user-guide/tui.md docs/user-guide/entrypoints.md
```

---

## Task 8: CI paths 与 fuzz 门禁收紧（裁定 2）

**Files:** Modify `.github/workflows/{docs,nightly,ci}.yml`

- [ ] **Step 1: 给 `docs.yml` 的 `paths` 补 `cmd/yanshi/**`**

⚠️ CLI 帮助快照的真相源是 `cmd/yanshi/{main,app,headless}.go` 的 FlagSet —— **改 flag 文案的 PR 不会触发 docs job，`help:*` 快照静默漂移**。

> **同一个列表还缺 `sdk/**`** —— W9 Task 8 负责。**先落地的一方一并加，另一方确认。**

- [ ] **Step 2: 去掉 `nightly.yml:18` 的 `continue-on-error`**

⚠️ 注释写着 "soft until E2 fuzz targets land"，而 `FuzzMatchGlob` **已实测存在** —— **前提条件已经成立**。

**第 38 行的 bench 保持软**（裁定 2）。

- [ ] **Step 3: 堵掉两个内部逃生口**

`nightly.yml` 的 fuzz-long 与 `ci.yml` 的 fuzz-seed 里都有 `soft-pass; exit 0` —— **两处都意味着「有人删光 fuzz/property 目标 → job 照样绿」**。

**改成「目标缺失即失败」。**

> E2 资产（`FuzzMatchGlob` + `internal/ctxcompact` 的 8 个 `TestProperty_*`）**已实测存在**；W4 还会再加。

- [ ] **Step 4: 提交**

---

## Task 9: 三份文档与仓库卫生（CONTRIB1 + EX1 + UDOC1）

**Files:** Modify `CONTRIBUTING.md`、`README.md`、`docs/archive/README.md`、`examples/sdk-python/main.py`（`:25`）、`.github/workflows/docs.yml`；Delete 仓库根的 4 个垃圾文件

- [ ] **Step 1: 删掉仓库根的 4 个已跟踪垃圾文件**

⚠️ **会随开源仓库暴露给所有人**：`demo.txt`（还是 dirty 状态）、`diffdemo.txt`（一段贴进来的 agent 输出）、`deps_analysis.md`、`deps_analyze.py`。

**复现**：`git ls-files | grep -v /`

⚠️ **`deps_analyze.py` 删前确认它是否还有用** —— 若有用，**移到 `scripts/` 并在 CONTRIBUTING 里登记**，不要直接删。

- [ ] **Step 2: CONTRIBUTING 补机器强制治理一节**

⚠️ W0 刚把 `governance` 变成硬门禁并新增 GOV4/5/6 + 台账断言，而 CONTRIBUTING 对 `internal/archtest`、`cmd/codelines`、三张豁免表、`cmd/featurestatus` **零字提及**；也没提「改 config/`-h`/schema 必须重跑四个生成器并提交」。

> **贡献者照着 CONTRIBUTING 走会在 governance/docs 两个 job 上撞墙。**

**顺带登记 `cmd/covercheck`**（Task 5）。

- [ ] **Step 3: 修 `docs/archive/README.md` 的两处可证伪陈述**

⚠️ 映射表只有 3 条但目录里有 7 个 md；末尾断言 synthesis-* 三份与 `deps_analysis` 「为未跟踪文件」—— **两处都假**（三份 synthesis-* 就在该目录且已被跟踪；`deps_analysis.md` 也被跟踪，只是躺在仓库根）。`feature-roadmap-e-h.md` 在目录里却不在表里。

- [ ] **Step 4: README 补五个入口链接**

⚠️ 实测根 README.md（254 行）对 `docs/user-guide`、`docs/api`、`docs/adr`、`examples/`、`CONTRIBUTING.md` 的引用数**全部为 0**（只提了 `docs/skills-authoring.md` 与 `docs/vcs.md`）。

> **开源发布后陌生人从 README 找不到用户指南、贡献指南、示例。**

- [ ] **Step 5: 修 `examples/sdk-python/main.py:25`**

⚠️ `item.toolName` 不是属性 —— `generated.py:121` 是 `tool_name` + pydantic alias，**alias 只作输入键** → **运行期必抛 `AttributeError`**。

> **同一个 bug 也在 `docs/api/sdk-python.md`** —— **W9 Task 7 Step 2 负责那一处**。先落地的一方两处一起改。

- [ ] **Step 6: 把 `goalloop-config/run.sh` 接进 docs.yml**

它**存在且自带断言**（grep `decision:`），但**不在 docs.yml 任何一步里**。

- [ ] **Step 7: 提交**

---

## Task 10: 台账翻牌 + W10 收尾 + S0 完成验收

- [ ] **Step 1: 翻牌 7 条**

| 条目 id | 现 verdict | 证据来自 |
|---|---|---|
| `E1/COV2` | partial | Task 5（门禁）+ Task 6（golden 全覆盖 + 去重言式） |
| `E1/COV3` | partial | Task 5 + Task 6（`WorkRoot` seam） |
| `H1/PKG1` | partial | Task 2（snapshot 真跑）+ Task 1（LICENSE 进归档） |
| `H1/VER1` | partial | Task 2 + Task 3 + Task 4 |
| `H2/CONTRIB1` | partial | Task 9 |
| `H2/EX1` | partial | Task 9 |
| `H2/UDOC1` | partial | Task 7（`-h` 快照）+ Task 8（paths）+ Task 9（README） |

⚠️ **`H2/UDOC1` 依赖 W8** —— `tui.md` 与 `configuration.md` 仍宣称 `/keymap` `/vim` `/contrast` 可用。**W8 未落地则 UDOC1 保持 `partial`。**

- [ ] **Step 2: 修 spec 与 `build.sh` 对 tag 的错误描述**

⚠️ **spec §5.1 与 VER1 证据都称「只有 m1..m9 里程碑 tag」—— 实际本仓库一个 tag 都没有。**

`build.sh` 与 `docs/upgrade-guide.md:64` 里「里程碑标签 m1..m9 被刻意跳过」的表述**目前是对不存在对象的描述**。

- [ ] **Step 3: S0 完成验收**

```bash
go build ./... && go vet ./... && go test ./...
go test -race ./...
go run ./cmd/codelines
go run ./cmd/covercheck
go run ./cmd/featurestatus
go test ./internal/archtest
```

**必须全部满足**：
- [ ] 台账 **63 条全部终态**（`done` + `removed`）
- [ ] **三张豁免表全空**（`assemblyExceptions` / `ctxInjectExceptions` / `toolWiringExceptions`）
- [ ] `lineExceptions` **无新增条目**
- [ ] 四个文档生成器跑完 `git diff --exit-code docs/` 无输出
- [ ] `goreleaser release --snapshot` **产物齐全且二进制能跑**

> 💡 **W0 已埋了 `TestS0CompletionGate`**：台账终态 == 63 时它会**硬断言三张表全空**。若它红了，**说明某个包没做完** —— 按它点名的条目回去补，**不要改断言**。

- [ ] **Step 4: 提交**

---

## W10 验收清单

- [ ] `LICENSE` 存在，且**在 goreleaser 归档里**
- [ ] `SECURITY.md` **无 placeholder 字样**
- [ ] `goreleaser release --snapshot --clean` 产出 4 个归档 + checksums，**解包后 `./yanshi -h` 退出 0**
- [ ] `release.yml` 的 CHANGELOG 生成**无 `--latest`**；changelog 回写**在 goreleaser 之后且可失败**
- [ ] `cliff.toml` 认 `^style`；`commit-convention.md` **不再提 `ide-vscode`**
- [ ] `go run ./cmd/covercheck` 可本地跑；`ci.yml` 有 coverage 硬门
- [ ] golden 覆盖**全部 34 个 Type**；`TestVocabulary_Symmetry` **不再是重言式**
- [ ] `TestBuild_VCSSoftDegrade` **真的制造了失败**
- [ ] `yanshi -h` 列出 **`pr` 与 `auth`**
- [ ] `docs.yml` 的 `paths` 含 `cmd/yanshi/**`（与 `sdk/**`）
- [ ] `nightly.yml:18` **无 `continue-on-error`**；两个 `soft-pass` 逃生口**已堵**
- [ ] 仓库根**无 `demo.txt` / `diffdemo.txt` / `deps_analysis.md` / `deps_analyze.py`**
- [ ] README 有**五个入口链接**
- [ ] `examples/sdk-python/main.py` **不再抛 `AttributeError`**
- [ ] **S0 完成验收全部通过**

## 依赖与移交

| 事项 | 关系 |
|---|---|
| `H2/UDOC1` | **依赖 W8** —— `tui.md`/`configuration.md` 的 `/keymap` 等宣称 |
| `docs.yml` paths 补 `sdk/**` | **与 W9 Task 8 重叠**，先落地的一方一并加 |
| `sdk-python.md` 的 `toolName` | **与 W9 Task 7 重叠**，两处一起改 |
| 协议选择与安全联系方式 | **产品决策** —— 不确定就停下来汇报 |
| ubuntu 侧覆盖率阈值复核 | 首次 CI 运行后 |

## 审计 / spec 中已被证伪的论断（不要照着做）

1. ⚠️ **审计 COV3 的「23%」与 COV2 的「67%」都是陈旧数**（实测 94.1% / 100%），来源 `docs/archive/synthesis-report-v2.md:343` 的 2026-07 快照。**spec §4.3 W10 写成「覆盖率门禁入 CI」略微掩盖了真实缺口是测试语义。**
2. **审计与 spec 都只记了 `auth` 一个隐藏子命令，实际是 `auth` + `pr` 两个。**
3. ⚠️ **spec §5.1 与 VER1 证据都称「只有 m1..m9 里程碑 tag」—— 实际一个 tag 都没有。**
4. **审计 COV2 说「31 个 `ServerFrame` Type」—— AST 实测是 34。** 但它指出的两个真实弱点**成立且已复现**。
5. ⚠️ **spec §4.3 W10 漏了两项发布硬阻塞**：LICENSE 不存在、SECURITY.md 联系方式仍是自标 placeholder。**按 §2 约束 4「开源产品标准」，这两条比表内任何一项都更该在 W10 收口。**
6. **`docs/archive/README.md` 末段关于 synthesis-* / `deps_analysis` 的两句是可证伪的错误陈述。**
7. **审计已记 `nightly.yml:38` 注释过期，但没记 `.bench-results/` 不在 `.gitignore`**（W7 Task 6 Step 4 处理）。
